name: 🛒 Stock Tracker

on:
  schedule:
    # Кожні 30 хвилин з 6:00 до 22:00 UTC (8:00–00:00 за Києвом)
    - cron: "0,30 6-22 * * *"
  workflow_dispatch:  # дозволяє запускати вручну

jobs:
  check_stock:
    runs-on: ubuntu-latest

    steps:
      - name: 🔹 Клонування репозиторію
        uses: actions/checkout@v4

      - name: 🐍 Встановлення Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Встановлення залежностей
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🕵️ Перевірка товарів
        env:
          RUN_ONCE: "true"                     # один прохід у GitHub режимі
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python stock_tracker.py

      - name: 🧾 Збереження зміненого products.json
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add products.json || echo "Немає змін"
          git diff --cached --quiet || git commit -m "🔄 update products.json [auto]"
          git push
