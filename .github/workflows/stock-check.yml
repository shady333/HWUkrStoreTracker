name: Stock Tracker

on:
  schedule:
    # Кожні 30 хв з 6:00 до 22:00
    - cron: "0,30 6-21 * * *"
  workflow_dispatch: # можна вручну запустити з GitHub UI

jobs:
  run-tracker:
    runs-on: ubuntu-latest

    steps:
      - name: 🔽 Checkout репозиторію
        uses: actions/checkout@v4

      - name: 🐍 Встановлення Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Встановлення залежностей
        run: |
          python -m pip install --upgrade pip
          pip install aiohttp beautifulsoup4 python-telegram-bot==20.3

      - name: 🚀 Запуск stock_tracker
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          RUN_ONCE: "true"
        run: |
          python stock_tracker.py

      - name: 📝 Перевірка змін у файлах
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add last_check.txt stock_tracker.log products.json || true
          if git diff --cached --quiet; then
            echo "✅ Немає змін — коміт не потрібен"
          else
            git commit -m "🕒 Автооновлення статусу товарів"
            git push
          fi
